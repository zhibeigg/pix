# 像素直出

后处理统一顺序：**perfectPixel → 抠背景（唯一一道，早期，含封闭区域）→ 按 alpha 裁剪主体 → 下采样 → 限制颜色数 → 描边/羽化（用户选了才有）→ 最后定版到标准尺寸 → 返回**

- 抠背景只做一道（早期），其产出的 alpha 用于定位/裁剪主体，并经量化保留下来。
- 描边/羽化在量化后、最终分辨率上单独处理；下采样阶段已为描边预留透明边距，描边不会超出标准尺寸被裁。
- 「定版到标准尺寸」：内容按比例适配到固定尺寸（如 64x64 / 32x32），不裁切主体，多余画布用透明像素填充。

## 无参考图

根据用户参数构建 prompt -> 生图 -> perfectPixel -> 抠背景（含封闭区域）-> 裁剪主体 -> 下采样 -> 限制颜色数 -> 描边/羽化（用户选了才有）-> 最后定版到标准尺寸（不裁主体，多余透明填充）-> 返回给用户

## 有参考图

根据用户参数构建 prompt（注入「图1 / 参考图」别名，用户可在主题或额外风格描述里用「参考图 / 图1」指代上传图）-> 上传参考图并生图 -> perfectPixel -> 抠背景（含封闭区域）-> 裁剪主体 -> 下采样 -> 限制颜色数 -> 描边/羽化（用户选了才有）-> 最后定版到标准尺寸（不裁主体，多余透明填充）-> 返回给用户

# 序列帧

帧尺寸为**自适应统一尺寸**：宽度按请求、高度按内容自适应，同一张表内各帧统一、底部锚定、多余画布透明填充（站立角色等竖向主体不会被压扁；如请求 64x64 的站立角色实际可能交付 64x128）。meta.json / sequence.json 会用 `delivered_frame_size` 与 `frame_size_adapted` 显式标注实际交付帧尺寸。

## 无参考图

根据用户参数构建 prompt -> 生图（整张 sheet）-> 用前景投影把源图切成每行动作、再切出每一帧 -> 对每一帧做 perfectPixel -> 对每一帧抠背景（含封闭区域，显式 key 色 pixel_bg 双阈值 alpha）-> 对每一帧按 alpha bbox 裁剪主体 -> 共享调色板统一限制颜色数 -> 对每一帧做描边/羽化（用户选了才有）-> 跨帧统一到自适应尺寸（底部锚定、透明填充）-> 合并回序列帧（横向 sheet + 网格预览 + sequence.json）-> 返回给用户

## 有参考图

根据用户参数构建 prompt（上传的图作为角色主体，每个 cell 复用同一角色设计）-> 上传参考图并生图 -> 用前景投影把源图切成每行动作、再切出每一帧 -> 对每一帧做 perfectPixel -> 对每一帧抠背景（含封闭区域）-> 对每一帧按 alpha bbox 裁剪主体 -> 共享调色板统一限制颜色数 -> 对每一帧做描边/羽化（用户选了才有）-> 跨帧统一到自适应尺寸（底部锚定、透明填充）-> 合并回序列帧 -> 返回给用户

# 本地像素化

不提供尺寸选择：输出尺寸自动按 perfectPixel 检测到的真实像素网格确定。

对上传图做 perfectPixel -> 抠背景（含封闭区域）-> 裁剪主体 -> 限制颜色数 -> 描边/羽化（用户选了才有）-> 定版（不裁主体，多余透明填充）-> 返回给用户
