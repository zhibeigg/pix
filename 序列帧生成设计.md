# 序列帧生成设计

## 1. 结论

当前方案总体可行：先生成第一帧，再基于上一帧图生图逐帧推进，最后统一后处理并横向合成为精灵表，适合角色动作、技能特效、道具动效这类需要连续性的序列帧。

但原方案有几个需要补强的点：

1. 只依赖上一帧会产生累计漂移，角色外观、朝向、比例、轮廓可能逐帧变化。
2. “取最大像素图片为标准”需要定义清楚：不能直接用原始生成图尺寸，而应使用后处理后的透明内容包围盒计算统一有效单帧尺寸。
3. 缺少明确的单帧尺寸、锚点、对齐方式、帧编号、帧坐标范围和输出清单。
4. “asset 流程”“asset 后处理流程”“perfectPixel 处理”的具体作用边界需要写清楚。
5. 作品库应以序列帧播放器为主，不强制依赖 GIF；GIF 只作为可选兼容导出。

推荐将该设计定义为“逐帧图生图生成 + 统一标准化后处理 + 横向精灵表导出 + 作品库按帧播放”的流程。

## 2. 已确认决策

根据当前确认，采用以下默认策略：

- 作品库预览：序列帧优先，不默认生成 GIF。
- GIF：仅作为可选兼容导出，不作为主展示格式。
- 越界策略：如果后处理后的透明内容超出目标单帧尺寸，扩大所有帧的有效单帧尺寸，并重新计算整张精灵表尺寸。
- 图生图参考：优先使用“第一帧 + 上一帧”双参考。
  - 第一帧用于锁定角色身份、配色、轮廓和整体风格。
  - 上一帧用于保持动作连续性。
  - 如果模型只支持单图参考，则退化为只使用上一帧，并在 prompt 中强锁第一帧身份特征。
- 播放源：作品库优先读取 `sprite_sheet.png + sequence.json`，独立帧图作为调试和导出产物保留。
- 最终后处理：复用当前 asset 后处理流程；序列帧流程只负责统一调用、记录实际步骤、计算有效单帧尺寸并透明补齐。
- 有效尺寸取整：当需要扩大有效单帧尺寸时，向上取整到 16 像素步长。
- 异常阈值：如果某帧透明内容宽或高超过目标尺寸的 150%，判定为异常帧并优先重新生成，而不是直接扩大整表。
- 计费规则：序列帧设置“单帧基础价”，用户可自定义帧数，帧数上限为 12 张，点数价格 = 帧数 × 单帧基础价。

## 3. 目标

生成一组连续动作序列帧，并导出为：

- 独立序列帧图片：`frame_001.png` 到 `frame_N.png`
- 横向精灵表：`sprite_sheet.png`
- 序列帧播放清单：`sequence.json`
- 生成记录：`00_input.txt`

作品库展示时优先读取横向精灵表和 `sequence.json` 进行逐帧播放，不强制生成 GIF。

## 4. 核心输入

### 4.1 用户输入

用户至少需要提供：

- 动作主体描述，例如“像素风火焰法师释放火球”
- 动作描述，例如“从蓄力到释放再到收招”
- 总帧数 `frame_count`，用户可自定义，但必须在 1 到 12 之间
- 目标单帧像素尺寸 `target_frame_width × target_frame_height`
- 播放帧率 `fps`

可选输入：

- 面向方向，例如“朝右”“朝左”“正面”
- 角色锚点，例如“底部居中”“中心点”“脚底中心”
- 是否循环播放
- 是否允许导出兼容 GIF
- 背景色或透明背景规则
- 动作关键帧说明

系统需要提供的配置：

- 单帧基础价 `frame_base_price`
- 最大帧数 `max_frame_count = 12`

### 4.2 推荐默认值

如果用户没有指定，可使用以下默认值：

- `frame_count = 9`
- `max_frame_count = 12`
- `fps = 8`
- `target_frame_size = 96 × 96`
- `anchor = bottom_center`
- `background = transparent`
- `playback = sprite_sheet_with_sequence_json`
- `gif_export = false`
- `overflow_policy = expand_sheet_to_fit`
- `frame_size_step = 16`
- `oversize_regenerate_threshold = 150%`
- `final_postprocess = reuse_asset_pipeline`

## 5. 计费规则

序列帧按帧数计费，需要配置一个“单帧基础价”：

```text
frame_base_price = 配置中的单帧基础价
frame_count = 用户选择的帧数，范围 1 到 12
total_points = frame_count × frame_base_price
```

规则要求：

1. 用户可以自定义 `frame_count`。
2. `frame_count` 必须是整数，最小值为 1，最大值为 12。
3. 前端和后端都必须校验帧数上限，不能只依赖前端限制。
4. 当用户输入超过 12 帧时，应阻止提交并提示“序列帧最多支持 12 帧”，不应自动扣费。
5. 点数价格只由帧数和单帧基础价决定：`total_points = frame_count × frame_base_price`。
6. 如果后续因为重试、后处理、尺寸扩大或生成 `sprite_sheet.png` 产生额外内部处理，不再额外向用户计费。
7. 如果生成失败且没有产出可用序列帧，应按现有失败退款或不扣费策略处理。

示例：

```text
单帧基础价：5 点
用户选择帧数：8
最终价格：8 × 5 = 40 点
```

`00_input.txt` 和 `sequence.json` 都应记录本次生成的计费快照，避免后续基础价变化影响历史记录。

## 6. 关键尺寸合同

序列帧生成必须先确定目标单帧尺寸，后续所有 prompt、后处理和导出都围绕该尺寸执行。

这里需要区分两个尺寸：

- `target_frame_size`：用户期望的目标单帧尺寸，用于构建 prompt 和初始后处理合同。
- `effective_frame_size`：最终实际用于所有帧和横向精灵表的统一单帧尺寸。

默认情况下：

```text
effective_frame_width  = target_frame_width
effective_frame_height = target_frame_height
```

如果某一帧经过最终后处理后，透明内容包围盒超出目标单帧尺寸，则不裁切主体，改为扩大所有帧的有效单帧尺寸：

```text
effective_frame_width  = max(target_frame_width,  max_content_width)
effective_frame_height = max(target_frame_height, max_content_height)
```

有效单帧尺寸扩大时，默认向上取整到 16 像素步长：

```text
frame_size_step = 16
effective_frame_width  = ceil_to_step(effective_frame_width,  frame_size_step)
effective_frame_height = ceil_to_step(effective_frame_height, frame_size_step)
```

注意：

1. prompt 仍然应强制要求模型尽量生成在 `target_frame_size` 内。
2. 后处理不能裁切主体内容。
3. 扩大尺寸时，必须扩大所有帧，而不是只扩大单帧。
4. 扩大后的有效单帧尺寸默认向上取整到 16 像素步长。
5. 如果某帧透明内容宽或高超过目标尺寸的 150%，优先判定为异常帧并重新生成。
6. 最终精灵表尺寸必须使用 `effective_frame_size` 计算。
7. `00_input.txt` 和 `sequence.json` 必须同时记录 `target_frame_size` 与 `effective_frame_size`。

示例：

```text
目标单帧尺寸：96 × 96 px
最终有效单帧尺寸：112 × 96 px
总帧数：10
横向精灵表逻辑尺寸：1120 × 96 px
读取顺序：从左到右，frame_001 到 frame_010
每帧坐标：
frame_001: x=0,    y=0, w=112, h=96
frame_002: x=112,  y=0, w=112, h=96
frame_003: x=224,  y=0, w=112, h=96
...
frame_010: x=1008, y=0, w=112, h=96
```

## 7. Prompt 设计

### 7.1 第一帧 prompt

第一帧使用 asset 流程生成，但 prompt 必须声明它是序列帧的起始帧，而不是普通单图。

第一帧 prompt 应包含：

- 主体描述
- 像素风限制
- 目标单帧尺寸合同
- 透明背景要求
- 面向方向
- 锚点要求
- 第 1 帧在完整动作中的阶段
- 禁止项

示例结构：

```text
Create frame 1 of a {frame_count}-frame pixel-art animation sequence.
Subject: {normalized_description}.
Action phase: frame 1 is the starting pose / anticipation pose.
Canvas contract: one single frame only, target frame size {target_frame_width}x{target_frame_height} px.
The character/effect should fit inside this frame whenever possible.
Background must be transparent or a flat removable background color.
Anchor: keep the subject aligned to {anchor}.
Style: crisp pixel art, hard edges, limited palette, no painterly blending, no anti-aliased soft brush.
Do not draw a sprite sheet, do not draw multiple frames, do not add text, watermark, UI, border, grid, or shadows outside the sprite.
```

### 7.2 后续帧 prompt

第 2 帧到第 N 帧使用图生图生成。优先同时使用第一帧和上一帧作为参考图。

每一帧 prompt 应包含：

- 当前帧编号 `frame_index`
- 总帧数 `frame_count`
- 该帧动作阶段
- 与上一帧的变化量
- 需要保持不变的内容
- 目标单帧尺寸合同
- 禁止项

示例结构：

```text
Generate frame {frame_index} of {frame_count} in the same pixel-art animation sequence.
Use the first frame as the identity/style reference.
Use the previous frame as the motion-continuity reference.
Keep the same character identity, costume, palette, outline thickness, camera, scale, facing direction, and anchor.
Action phase: {frame_action_description}.
Motion delta from previous frame: {motion_delta_description}.
Canvas contract: one single frame only, target frame size {target_frame_width}x{target_frame_height} px.
The subject should remain inside the frame and aligned to {anchor}.
Do not create a grid, collage, sprite sheet, duplicate character, text, watermark, background scene, or extra frames.
```

如果图生图模型只支持一张参考图，使用上一帧作为参考，并在 prompt 中额外写明第一帧的稳定特征，例如服装、轮廓、主色、武器、发型、体型和朝向。

### 7.3 动作阶段生成

如果用户只提供整体动作描述，系统应先生成内部动作时间线，再逐帧生成。

例如总帧数为 10 时：

```text
frame_001: 起始待机姿势，能量尚未出现
frame_002: 手部开始抬起，出现少量能量粒子
frame_003: 蓄力增强，身体略微后倾
frame_004: 能量球成型，轮廓更亮
frame_005: 释放前最高蓄力点
frame_006: 火球向前推出，身体重心前移
frame_007: 火球离手，拖尾展开
frame_008: 释放后的余焰和收招动作
frame_009: 角色恢复站姿，残留少量粒子
frame_010: 回到可循环的结束姿势
```

这份动作时间线必须写入 `00_input.txt`，方便复现和排查。

## 8. 推荐生成流程

### 8.1 阶段 A：参数标准化

1. 读取用户原始 prompt。
2. 标准化主体描述 `normalized_description`。
3. 解析或补全：
   - `frame_count`
   - `target_frame_width`
   - `target_frame_height`
   - `fps`
   - `anchor`
   - `facing_direction`
   - `loop`
   - `overflow_policy`
   - `frame_size_step`
   - `oversize_regenerate_threshold`
   - `frame_base_price`
   - `max_frame_count`
4. 校验 `frame_count` 是否在 1 到 12 之间，并计算 `total_points = frame_count × frame_base_price`。
5. 生成每帧动作阶段描述。
6. 先按 `target_frame_size` 计算初始横向精灵表尺寸和每帧坐标范围。

### 8.2 阶段 B：生成第一帧

1. 使用序列帧专属 asset prompt 生成第一帧。
2. 对第一帧执行参考用预处理：
   - `perfectPixel`
   - 必要的尺寸检查
   - 必要的轻量裁剪或居中
3. 保存：
   - `frames/raw/frame_001.png`
   - `frames/reference/frame_001.png`

注意：参考帧不一定要提前做最终透明化。如果图生图模型对透明通道支持不好，可以保留一份扁平背景参考图，同时最终输出仍在后处理阶段透明化。

### 8.3 阶段 C：逐帧图生图

从第 2 帧到第 N 帧循环：

1. 读取第一帧参考图。
2. 读取上一帧参考图。
3. 构建当前帧 prompt。
4. 使用图生图生成当前帧。
5. 执行参考用 `perfectPixel` 处理。
6. 做基础质量检查：
   - 是否仍为单个主体
   - 是否保持同一角色身份
   - 是否明显越界
   - 是否出现文字、边框、网格、背景场景
   - 是否与上一帧动作连续
7. 保存当前帧 raw 和 reference 版本。

双参考策略：

- 第一帧用于锁定角色身份、配色和轮廓。
- 上一帧用于保持动作连续。

### 8.4 阶段 D：统一后处理

所有帧生成完成后，再统一复用当前 asset 后处理流程。序列帧流程不重新定义 asset 后处理内部细节，只负责统一调用并记录实际执行步骤。

推荐顺序：

1. 执行当前 asset 后处理流程。
2. 记录 asset 后处理实际步骤，例如 `perfectPixel`、背景透明化或 `Color-to-Alpha`、去噪等。
3. 计算每帧透明内容包围盒。
4. 汇总所有帧的最大内容宽高。
5. 如果某帧透明内容宽或高超过目标尺寸的 150%，优先判定为异常帧并重新生成。
6. 根据 `target_frame_size`、最大内容宽高和 `overflow_policy = expand_sheet_to_fit` 计算 `effective_frame_size`。
7. 将 `effective_frame_size` 向上取整到 16 像素步长。
8. 按锚点将每帧对齐到 `effective_frame_size`。
9. 多余画布填充透明像素。
10. 输出最终标准帧。

最终标准帧必须满足：

- 所有帧宽度都等于 `effective_frame_width`
- 所有帧高度都等于 `effective_frame_height`
- 背景透明
- 主体不越界
- 同一动作序列内锚点一致
- 颜色风格和像素颗粒度一致

### 8.5 阶段 E：合成横向精灵表

将最终标准帧按编号从左到右横向排列：

```text
sprite_sheet_width  = effective_frame_width × frame_count
sprite_sheet_height = effective_frame_height
```

排列顺序：

```text
frame_001, frame_002, frame_003, ..., frame_N
```

每帧坐标范围写入 `sequence.json` 和 `00_input.txt`。

### 8.6 阶段 F：作品库播放

作品库使用序列帧播放器：

1. 读取 `sequence.json`。
2. 加载 `sprite_sheet.png`。
3. 根据每帧 `sheet_rect` 从横向精灵表中裁切显示。
4. 按 `fps` 和 `frame_count` 逐帧播放。
5. 支持暂停、循环、倍率和透明背景预览。

兼容导出策略：

```text
gif_export = false by default
```

只有用户需要下载 GIF 或旧流程兼容时才生成 GIF。

### 8.7 阶段 G：帧锚点手动调整

生成完成后，作品库需要提供“调整锚点”能力，用于修正逐帧图生图带来的主体漂移。该阶段不重新调用 AI，也不额外扣点，只对已经生成的透明帧进行本地重合成。

交互要求：

1. 用户可选择任意一帧，并拖动当前帧主体在有效画布中的位置。
2. 主编辑画布显示棋盘格背景、当前帧全不透明图像，以及 onion-skin 半透明参考影子。
3. 普通帧默认显示上一帧半透明影子；第 1 帧显示最后一帧；最后一帧显示第 1 帧，便于检查闭环衔接。
4. 拖动时右侧实时播放应用偏移后的序列帧效果。
5. 支持数字输入 `offset_x / offset_y`、重置当前帧、重置全部、复制参考帧偏移、底部中心对齐到参考帧、底部中心对齐到第 1 帧。
6. 保存后写入 `alignments/{version}/`，并把作品库活跃输出切换到调整版本。

保存请求示例：

```json
{
  "fps": 8,
  "gif_export": true,
  "frames": [
    { "index": 1, "offset_x": 0, "offset_y": 0 },
    { "index": 2, "offset_x": -2, "offset_y": 3 }
  ]
}
```

## 9. 输出文件结构

推荐结构：

```text
run_dir/
  00_input.txt
  sequence.json
  sprite_sheet.png
  frames/
    raw/
      frame_001.png
      frame_002.png
      ...
    reference/
      frame_001.png
      frame_002.png
      ...
    final/
      frame_001.png
      frame_002.png
      ...
  alignments/
    20260526-235959/
      alignment.json
      sequence.json
      sprite_sheet.png
      sprite.gif
      frames/
        frame_001.png
        frame_002.png
        ...
```

`frames/final/` 是原始最终帧，不应被覆盖；`alignments/{version}/` 是用户手动调整锚点后生成的非破坏式版本。

## 10. `sequence.json` 建议字段

```json
{
  "type": "sequence_frames",
  "frame_count": 10,
  "max_frame_count": 12,
  "billing": {
    "frame_base_price": 5,
    "total_points": 50,
    "formula": "frame_count * frame_base_price"
  },
  "fps": 8,
  "loop": true,
  "target_frame_size": {
    "width": 96,
    "height": 96
  },
  "effective_frame_size": {
    "width": 112,
    "height": 96
  },
  "sheet_size": {
    "width": 1120,
    "height": 96
  },
  "anchor": "bottom_center",
  "overflow_policy": "expand_sheet_to_fit",
  "frame_size_step": 16,
  "oversize_regenerate_threshold": 1.5,
  "final_postprocess": "reuse_asset_pipeline",
  "playback_source": "sprite_sheet.png",
  "alignment": {
    "version": "20260526-235959",
    "coordinate_space": "effective_frame_size",
    "unit": "pixel",
    "active": true,
    "frames": [
      { "index": 1, "offset_x": 0, "offset_y": 0 },
      { "index": 2, "offset_x": -2, "offset_y": 3 }
    ]
  },
  "frames": [
    {
      "index": 1,
      "name": "frame_001",
      "file": "frames/final/frame_001.png",
      "sheet_rect": { "x": 0, "y": 0, "w": 112, "h": 96 },
      "action_phase": "起始待机姿势"
    },
    {
      "index": 2,
      "name": "frame_002",
      "file": "frames/final/frame_002.png",
      "sheet_rect": { "x": 112, "y": 0, "w": 112, "h": 96 },
      "action_phase": "动作开始"
    }
  ]
}
```

## 11. `00_input.txt` 必须记录的内容

`00_input.txt` 不能只保存用户原始 prompt，应至少包含：

```text
[raw_prompt]
用户原始输入

[normalized_description]
标准化后的主体和动作描述

[sequence_settings]
frame_count = 10
max_frame_count = 12
fps = 8
loop = true
target_frame_width = 96
target_frame_height = 96
effective_frame_width = 112
effective_frame_height = 96
anchor = bottom_center
facing_direction = right
background_policy = transparent
overflow_policy = expand_sheet_to_fit
frame_size_step = 16
oversize_regenerate_threshold = 150%
final_postprocess = reuse_asset_pipeline
gif_export = false
playback_source = sprite_sheet_with_sequence_json
reference_policy = first_frame_and_previous_frame

[billing]
frame_base_price = 5
total_points = 50
formula = frame_count * frame_base_price
billing_note = retries/postprocess/sprite_sheet_packaging do not add extra user-facing cost

[sheet_contract]
sprite_sheet_width = 1120
sprite_sheet_height = 96
read_order = left_to_right

[frame_bounds]
frame_001 = x:0, y:0, w:112, h:96
frame_002 = x:112, y:0, w:112, h:96
...

[action_timeline]
frame_001 = ...
frame_002 = ...
...

[effective_prompt_frame_001]
完整第一帧 prompt

[effective_prompt_frame_002]
完整第二帧 prompt

[processing_pipeline]
frame_generation = first_frame_asset_generation_then_iterative_img2img
reference_policy = first_frame_and_previous_frame
reference_preprocess = perfectPixel
final_postprocess = reuse current asset pipeline -> calculate content bounds -> transparent pad to effective frame size
packing = horizontal_sprite_sheet
preview = sprite_sheet_sequence_player
```

## 12. 质量检查规则

每帧生成后建议执行检查：

1. 单帧检查
   - 图片中只能有当前一帧，不允许出现九宫格、拼贴图或多帧表。
   - 不允许有文字、水印、边框、UI、网格。
   - 主体必须完整保留，不允许被裁切。

2. 连续性检查
   - 角色身份一致。
   - 配色一致。
   - 朝向一致。
   - 像素颗粒度一致。
   - 轮廓粗细一致。
   - 动作相邻帧变化合理，不能突变。

3. 尺寸检查
   - 最终每帧尺寸必须一致。
   - 横向精灵表尺寸必须等于 `effective_frame_width × frame_count` 和 `effective_frame_height`。
   - 每帧坐标必须能被有效单帧尺寸整除。
   - 如果 `effective_frame_size` 大于 `target_frame_size`，必须在 `00_input.txt` 中记录扩大原因。

4. 透明度检查
   - 背景必须透明。
   - 主体边缘不应出现明显脏边。
   - 不应残留纯色底或背景块。

5. 计费检查
   - `frame_count` 必须在 1 到 12 之间。
   - `total_points` 必须等于 `frame_count × frame_base_price`。
   - `sequence.json` 与 `00_input.txt` 中记录的计费快照必须一致。
   - 重试、后处理、尺寸扩大和精灵表合成不能额外增加用户点数消耗。

## 13. 失败处理建议

如果某一帧失败，不建议直接继续生成后续帧，否则错误会扩散。

推荐策略：

1. 对失败帧重新生成，最多重试 `max_retries` 次。
2. 重试时强化 prompt 中的禁止项和尺寸合同。
3. 如果仍失败，回退到最近通过质量检查的参考帧。
4. 如果动作漂移严重，可重新生成动作时间线或要求用户补充关键帧描述。
5. 如果内容超出目标单帧尺寸但主体质量合格，按 `overflow_policy = expand_sheet_to_fit` 扩大所有帧的有效单帧尺寸，不裁切主体。
6. 如果内容超出过多，例如超过目标尺寸的 150%，判定为异常帧并重新生成，避免整张精灵表被单个异常帧撑得过大。

## 14. 对原方案的具体优化

原方案：

```text
第一帧 asset 生成 -> perfectPixel -> 图生图第二帧 -> perfectPixel -> 图生图第三帧 -> ... -> asset 后处理 -> 取最大尺寸 -> 透明补齐 -> 横向合成 -> 作品库序列帧播放
```

建议优化为：

```text
参数标准化 -> 动作时间线 -> 第一帧 asset 生成 -> 参考用 perfectPixel ->
使用第一帧+上一帧逐帧图生图并质量检查 -> 全部帧统一最终后处理 ->
计算 effective_frame_size -> 按有效尺寸透明补齐 -> 横向合成精灵表 ->
生成 sequence.json -> 作品库按 sprite_sheet + sequence.json 播放
```

关键变化：

- 用目标单帧尺寸约束生成，用有效单帧尺寸统一最终导出。
- 生成阶段和最终后处理阶段分离，避免透明通道影响图生图参考质量。
- 后续帧优先使用“第一帧 + 上一帧”双参考，降低累计漂移。
- 每帧 prompt 明确当前帧编号、总帧数、动作阶段和禁止项。
- 增加动作时间线，避免模型只知道“下一帧”但不知道整体动作进度。
- 增加 `sequence.json`，作品库直接读取精灵表坐标逐帧播放，不依赖 GIF。
- 记录完整有效 prompt、每帧坐标、目标尺寸、有效尺寸和尺寸扩大原因，方便复现。

## 15. 实现注意事项

以下策略已经明确，进入实现时需要按此落地：

1. 最终后处理复用当前 asset 后处理流程；序列帧流程负责统一调用、记录实际执行步骤、计算有效单帧尺寸并透明补齐。
2. `frame_size_step = 16`，扩大后的 `effective_frame_size` 必须向上取整到 16 像素步长。
3. `oversize_regenerate_threshold = 150%`，超过阈值的异常帧优先重新生成，不直接撑大整张精灵表。
4. 双参考图生图不可用时，允许自动退化为“只用上一帧 + prompt 锁定第一帧特征”，并在 `00_input.txt` 中记录退化原因。
5. 作品库优先读取 `sprite_sheet.png + sequence.json` 播放；独立帧图用于调试、二次处理和导出。
6. 序列帧计费按 `total_points = frame_count × frame_base_price` 执行，`frame_count` 上限固定为 12，前后端都必须校验。
