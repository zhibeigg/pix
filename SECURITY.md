# 安全政策

## 支持范围

Pix 仅为最新发布版本和 `master` 分支提供安全修复。旧版本出现问题时，请先升级到最新 Release 再确认是否仍可复现。

## 私密报告漏洞

**不要通过公开 Issue、Discussion、PR、日志或截图披露漏洞细节和凭据。**

请使用 GitHub 的 Private Vulnerability Reporting：

1. 打开 <https://github.com/zhibeigg/pix/security/advisories/new>；
2. 提供受影响版本、影响范围、复现步骤和可选修复建议；
3. 仅附最小必要的脱敏证据，不要提交真实用户数据或仍有效的 Key。

我们会尽量在 7 个工作日内确认报告。修复完成前，请避免公开利用细节。若私密报告入口不可用，可创建一个**不含任何漏洞细节**的普通 Issue，请求维护者开启私密联系渠道。

## 凭据泄漏

如果你发现 API Key、JWT Secret、数据库密码、支付密钥或其他凭据：

1. 立即停止传播，不要复制到公开渠道；
2. 在对应供应商或密钥系统中撤销并轮换；
3. 检查访问日志、账单和异常活动；
4. 清理当前文件和完整 Git 历史；
5. 使用 Gitleaks 重新扫描，并通知受影响的部署方更新配置。

仅从 Git 历史删除字符串并不能使凭据恢复安全，**轮换始终是必需步骤**。

## 项目安全基线

- 生产环境 `PIX_WEB_ENV=prod` 时，弱 JWT Secret 会导致启动失败；
- 用户文件和任务输入必须经过归属校验；
- 外部资源下载经过 SSRF 与重定向目标校验；
- 登录、验证码和敏感接口具有速率限制；
- CI 对完整 Git 历史执行 Gitleaks，并对 Python/npm 依赖执行漏洞审计；
- 发布制品和 GHCR 镜像附带校验和与构建 provenance。

安全设计不等于不存在漏洞。如果你能够绕过这些控制，请使用上述私密渠道报告。
