export type PixThemeMode = 'light' | 'dark'
export type PixThemePreference = 'light' | 'dark' | 'system'
export type PixLanguage = 'zh-CN' | 'en'

export const pixBrand = {
  cream: 'hsl(var(--pix-cream))',
  parchment: 'hsl(var(--pix-parchment))',
  ink: 'hsl(var(--pix-ink))',
  amber: 'hsl(var(--pix-amber))',
  mint: 'hsl(var(--pix-mint))',
  sky: 'hsl(var(--pix-sky))',
  lavender: 'hsl(var(--pix-lavender))',
  rose: 'hsl(var(--pix-rose))',
  navy: 'hsl(var(--pix-navy))',
}
